<h1>Create new bookmark to list <%= @list.name %></h1>
<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Add", class: "btn btn-dark" %>
<% end %>
